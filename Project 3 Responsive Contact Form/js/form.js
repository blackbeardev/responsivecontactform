var submitForm = document.getElementById("submitButton");

var submitMessage = function() {
  alert("Thanks for joining our mail-list!");
}

submitForm.addEventListener("submit", submitMessage);
